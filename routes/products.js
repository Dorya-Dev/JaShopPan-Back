const express = require('express');
const router = express.Router();
const user = require("../controllers/user");

/* GET home page. */

/*router.get('/content', user.title);

router.get('/all', user.title);
*/
  

module.exports = router;
